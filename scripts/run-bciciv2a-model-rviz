#!/usr/bin/env bash
set -euo pipefail
# shellcheck source=scripts/lib.sh
source "$(dirname -- "${BASH_SOURCE[0]}")/lib.sh"

enter_ros_workspace
exec ros2 launch eeg_bci_pipeline bciciv2a_model_robot_rviz.launch.py "$@"
